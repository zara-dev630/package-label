{
  "name": "packlabel-detector",
  "version": "1.0.0",
  "private": true,
  "description": "Packet label detector: React (Vite) frontend + Express/ONNX YOLOv8 API",
  "scripts": {
    "vercel-build": "cd client && npm ci && npm run build",
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "multer": "^2.2.0",
    "onnxruntime-node": "^1.27.0",
    "sharp": "^0.35.3"
  }
}
